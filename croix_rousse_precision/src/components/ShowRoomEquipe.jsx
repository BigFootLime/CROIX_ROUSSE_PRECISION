import { feedback } from "../constants";
import styles from "../style";
import FeedbackCard from "./FeedbackCard";

const ShowRoomEqipe = () => (
  <section
    id="clients"
    className={`${styles.paddingY} ${styles.flexCenter} hidden md:block md:h-[750px] md:flex-col md:relative `}
  ></section>
);

export default ShowRoomEqipe;
